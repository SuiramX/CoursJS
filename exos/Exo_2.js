let prix_objet = 100;
let isMember = true;

if (isMember) {
    let prix_final = prix_objet * 0.9;
    console.log(`Le prix final est de ${prix_final} euros`);
}
else{
    console.log(`Le prix final est de ${prix_objet} euros`);
}